import Link from "next/link";
import { login } from "@/app/auth-actions";
export default function LoginPage() { return <main className="shell" style={{ maxWidth: 460 }}><div className="pill">TRIPSPLIT</div><h1 style={{ fontSize: 34, marginBottom: 8 }}>Welcome back</h1><p className="muted">Sign in to manage your family trip finances.</p><form action={login} className="card" style={{ marginTop: 28 }}><div className="field"><label>Username</label><input name="username" autoComplete="username" required /></div><div className="field"><label>Password</label><input name="password" type="password" autoComplete="current-password" required /></div><button className="btn" style={{ width: "100%" }}>Sign in</button><p className="muted" style={{ textAlign: "center", fontSize: 14 }}>New here? <Link href="/signup">Create an account</Link></p></form></main>; }
