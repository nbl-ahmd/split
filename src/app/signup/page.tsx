import Link from "next/link";
import { signup } from "@/app/auth-actions";
export default function SignupPage() { return <main className="shell" style={{ maxWidth: 460 }}><div className="pill">TRIPSPLIT</div><h1 style={{ fontSize: 34, marginBottom: 8 }}>Create your account</h1><p className="muted">Use a username and password to keep trips private.</p><form action={signup} className="card" style={{ marginTop: 28 }}><div className="field"><label>Username</label><input name="username" autoComplete="username" minLength={3} required /></div><div className="field"><label>Email</label><input name="email" type="email" autoComplete="email" required /></div><div className="field"><label>Password</label><input name="password" type="password" autoComplete="new-password" minLength={8} required /><small className="muted">At least 8 characters.</small></div><button className="btn" style={{ width: "100%" }}>Create account</button><p className="muted" style={{ textAlign: "center", fontSize: 14 }}>Already registered? <Link href="/login">Sign in</Link></p></form></main>; }
