import Link from "next/link";
const trips = [{ name: "Kashmir Family Trip", destination: "Srinagar · 20–27 Aug 2026", total: "₹48,250", families: 4, status: "Active" }, { name: "Goa Weekend", destination: "Goa · 12–14 Dec 2025", total: "₹21,800", families: 3, status: "Completed" }];
export default function Home() { return <main className="shell"><header className="row" style={{ marginBottom: 32 }}><div><div className="pill">TRIPSPLIT</div><h1 style={{ fontSize: 34, margin: "13px 0 4px" }}>Your trips</h1><p className="muted" style={{ margin: 0 }}>Keep the group money simple.</p></div><Link className="btn" href="/trips/new">＋ New trip</Link></header><div className="grid">{trips.map((trip) => <Link href="/trips/demo" key={trip.name} className="card" style={{ color: "inherit", textDecoration: "none" }}><div className="row"><div><h2 style={{ margin: 0, fontSize: 20 }}>{trip.name}</h2><p className="muted" style={{ margin: "7px 0" }}>{trip.destination}</p></div><span className="pill">{trip.status}</span></div><div className="row" style={{ marginTop: 18 }}><span className="amount">{trip.total}</span><span className="muted">{trip.families} families ↗</span></div></Link>)}</div></main>; }
